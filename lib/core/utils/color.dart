import 'package:flutter/material.dart';

class AppColors {
  // Primary color used throughout the onboarding screen
  static const Color primary = Colors.blue;

  // Text colors
  static const Color titleText = Colors.black;
  static const Color descriptionText = Colors.grey;

  // Dot indicator colors
  static final Color inactiveDot = Colors.grey.shade300;
  static final Color bgColor = const Color.fromARGB(255, 237, 233, 233);

  // Button colors
  static const Color buttonText = Colors.white;
  static const Color buttonShadow = Colors.blue;
}
